version: "3.8"

services:
  totnet:
    image: totnet:dev
    container_name: totnet
    stdin_open: true
    tty: true
    # NOTE: 各自設定してください．
    volumes:
      - ../data:/opt/ml/data
      - ../src:/opt/ml/code/src
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    shm_size: 8GB
    # ports:
    #   - 8080:8080
    restart: always
